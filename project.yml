name: DataStreamApp
options:
  bundleIdPrefix: com.actrium
  deploymentTarget:
    iOS: "26.2"
settings:
  base:
    SUPPORTED_PLATFORMS: "iphoneos iphonesimulator"
packages:
  actr-swift:
    url: https://github.com/Actrium/actr-swift-package-sync
    exactVersion: "0.3.6"
  swift-protobuf:
    url: https://github.com/apple/swift-protobuf.git
    from: 1.32.0
schemes:
  DataStreamApp:
    build:
      targets:
        DataStreamApp: all
    run:
      config: Debug

targets:
  DataStreamApp:
    type: application
    platform: iOS
    sources:
      - path: DataStreamApp
      - path: actr.toml
        type: file
        buildPhase: resources
      - path: actr.lock.toml
        type: file
        buildPhase: resources
    dependencies:
      - package: actr-swift
        product: Actr
      - package: swift-protobuf
        product: SwiftProtobuf
    info:
      path: DataStreamApp/Info.plist
      properties:
        CFBundleDisplayName: DataStreamApp
        UILaunchScreen: {}
        NSLocalNetworkUsageDescription: DataStreamApp connects to the actrium signaling server for data stream testing.
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.actrium.DataStreamApp
        SWIFT_VERSION: "6.0"
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTED_PLATFORMS: "iphoneos iphonesimulator"
        CODE_SIGN_STYLE: Automatic
